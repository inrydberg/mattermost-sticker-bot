# Mattermost Sticker Bot Configuration

# ============================================
# OPTION 1: Local Development (localhost)
# ============================================
# Use this when Mattermost and bot run on the same machine
#
# MM_SERVER_URL=http://localhost:8065
# MM_WS_URL=ws://localhost:8065/api/v4/websocket
# DOMAIN=http://localhost
# MM_BOT_TOKEN=your_mattermost_bot_token_here
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
# UI_PORT=3333
# UI_HOST=0.0.0.0

# ============================================
# OPTION 2: Remote Deployment
# ============================================
# Use this when deploying bot separately from Mattermost server
#
# IMPORTANT: If Mattermost and the bot are deployed on SEPARATE servers,
# DOMAIN must be a public IP address or domain name that ALL users can access.
# The sticker picker web UI runs on this address - if users can't reach it,
# they won't be able to select stickers!
#
# CLOUD HOSTING NOTE (AWS / Azure / GCP / etc.):
# You MUST open UI_PORT (default 3333) in your security group / firewall rules:
#   - AWS: Security Groups → Inbound Rules → Add TCP port 3333
#   - Azure: Network Security Group → Inbound security rules → Add port 3333
#   - GCP: VPC Firewall Rules → Create rule for TCP port 3333
#
MM_SERVER_URL=https://your-mattermost-server.com
MM_WS_URL=wss://your-mattermost-server.com/api/v4/websocket
DOMAIN=http://your-sticker-bot-domain.com
MM_BOT_TOKEN=your_mattermost_bot_token_here
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
UI_PORT=3333
UI_HOST=0.0.0.0

# Bot Username (as configured in Mattermost, without @)
BOT_USERNAME=stickerbot
